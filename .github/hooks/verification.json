{
	"version": 1,
	"hooks": {
		"postToolUse": [
			{
				"type": "command",
				"matcher": "edit|create|str_replace_editor|apply_patch",
				"bash": "pnpm run check --fix >/dev/null 2>&1 || true",
				"timeoutSec": 30
			}
		],
		"agentStop": [
			{
				"type": "command",
				"bash": "out=$(pnpm run check-types 2>&1); status=$?; if [ \"$status\" -ne 0 ]; then jq -n --arg reason \"$out\" '{decision:\"block\", reason: $reason}'; fi",
				"timeoutSec": 60
			}
		]
	}
}
